'use client';
import { useState } from 'react';
import type { Study } from './studies';
import { ArrowIcon } from '../../components/arrow-icon';
export function Examples({study}:{study:Study}){
 const [selected,setSelected]=useState(0);const example=study.examples[selected];
 return <section className="sw-examples" id="try-it"><div className="sw-section-intro"><p className="sw-eyebrow">02 / Put it to work</p><h2>Same business.<br/>Different little jobs.</h2><p>Choose an example to see the brief, the material and a possible result. These are prepared demonstrations of what we could practise together.</p></div><div className="sw-example-tabs" role="group" aria-label="Choose a worked example">{study.examples.map((e,i)=><button key={e.label} aria-pressed={selected===i} onClick={()=>setSelected(i)}>{e.label}<ArrowIcon direction="right"/></button>)}</div><div className="sw-example-grid" aria-live="polite"><div className="sw-brief"><p className="sw-eyebrow">The starting material</p><h3>{example.fictional?'A practice conversation':'Facts we can work from'}</h3><ul>{example.facts.map(f=><li key={f}>{f}</li>)}</ul><div className="sw-instruction"><span>Your instruction</span><p>{example.brief}</p></div></div><div className={`sw-draft sw-draft-${example.kind}`}><p className="sw-eyebrow">{example.fictional?'Fictional training example':'Concept copy'} · For review</p><div className="sw-draft-brand">{study.short}</div><h3>{example.headline}</h3><p>{example.text}</p><div className="sw-draft-end"><span>{example.kind==='reply'?'Draft response':example.kind==='enquiry'?'Draft email':'Example creative'}</span><span aria-hidden="true"><ArrowIcon/></span></div></div></div><div className="sw-human-check"><span className="sw-eyebrow">Your judgement belongs here</span><p>{example.check}</p></div></section>;
}
