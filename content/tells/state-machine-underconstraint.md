+++
title = "State-Machine Underconstraint"
id = "state-machine-underconstraint"
type = "tells"
+++

Workflow test checks only the terminal state, not the transition sequence. System arrives at correct end state via wrong path. Assert the transition sequence, log state changes with timestamps.

**Ref:** Watchdog eval taxonomy 2026-03-19
