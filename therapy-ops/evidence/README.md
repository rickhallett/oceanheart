# Evidence

Only deterministic, synthetic proof belongs here.

`synthetic-e2e-proof.json` is generated by:

```sh
npm run proof
```

The proof uses fake Google Workspace adapters and makes no network calls. It
exists to prove workflow structure and safety gates before any real Workspace
deployment.

Do not add screenshots, exports, logs, or fixtures containing real client
information.
